subcomponents:
  myapp:
    config:
      zoo:
        zii: zbb
  myservice:
    subcomponents:
      mysubservice:
        config:
          foo:
            bar:
              k8.beta.io/load-balancer-group: foo-bar-updated
              line: solid
